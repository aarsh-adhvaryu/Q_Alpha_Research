# Streamlit Community Cloud entry point for the research hedge dashboard
# (scripts/hedge_dashboard_app.py). Streamlit Cloud installs from this file; `-e .` pulls this repo
# (which in turn resolves the qalpha engine via [tool.uv.sources]/pip) plus the UI + data deps.
-e .
streamlit>=1.37
yfinance>=0.2
